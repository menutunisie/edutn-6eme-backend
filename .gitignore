__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.env
*.db
*.db-shm
*.db-wal
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.log
.DS_Store
.idea/
.vscode/

# Fichiers uploades en local (jamais commites) ; .gitkeep garde le dossier
storage/*
!storage/.gitkeep

# Images sources du pipeline d'import de schemas (jamais commitees, gardees
# en place localement comme historique des imports deja effectues).
imports/
# Idem : dossier cree par erreur de frappe ("impotrs") a la place d'imports/,
# garde tel quel (contient de vraies images) mais jamais commite non plus.
impotrs/
